[Unit]
Description=Generate the shared palette from Linux Wallpaper Engine
After=graphical-session.target

[Service]
Type=oneshot
ExecStart=%h/.config/update-theme.py --wallpaper-engine --connector eDP-1

[Install]
WantedBy=default.target
